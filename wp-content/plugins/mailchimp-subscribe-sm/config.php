<?php

if ( ! defined( 'ABSPATH' ) ) exit;

define( 'SSM_PLUGIN_PATH', plugin_dir_path( __FILE__ ) );

define( 'SSM_PLUGIN_URL', plugins_url( '',plugin_basename( __FILE__ ) ) );

define( 'SSM_PLUGIN_DIR_NAME', dirname( plugin_basename( __FILE__ ) ) );



?>